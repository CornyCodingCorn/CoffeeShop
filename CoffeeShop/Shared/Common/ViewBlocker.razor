<div class="view-blocker @(Show ? "show" : "")">
    <div class="view-blocker-horizontal">
        @ChildContent
    </div>
</div>

@code {
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
    [Parameter]
    public bool Show { get; set; } = false;
}